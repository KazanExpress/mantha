import { VueConfiguration } from 'vue/types/vue';

export const errorHandler: VueConfiguration['errorHandler'] =
  (err, vm, info) => {

  };

export const warnHandler: VueConfiguration['warnHandler'] =
  (msg, vm, trace) => {

  };
